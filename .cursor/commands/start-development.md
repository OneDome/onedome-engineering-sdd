# start-development

Start implementation for an approved SDD task.

Follow `.cursor/skills/sdd-development/SKILL.md`.

Pass the Jira key (example: `DEAL-2097`). First run: propose DEV subtasks and stop. After `I approve the development breakdown`: persist state and create/reuse application branches. If already started: report status only (resume).
