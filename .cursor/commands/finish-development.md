# finish-development

Close implementation for an SDD task after every DEV subtask is accepted.

Follow `.cursor/skills/sdd-development/SKILL.md` (`/finish-development`).

Pass the Jira key. Does not merge, open application PRs, or change Jira status.
