# sdd-pr

Open a GitHub PR in `onedome-engineering-sdd` so the Jira ticket shows the spec/plan.

Follow `.cursor/skills/sdd-pr/SKILL.md`.

Pass the Jira key if known (example: `DEAL-2097`). Otherwise use the key in `specs/*/task-state.yaml` for the task in this chat.
